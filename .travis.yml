language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t bkern98/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bkern98/docker-react npm run test
